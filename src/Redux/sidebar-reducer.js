const initialState = {
    friendsItem: [
        {id: 1, name: 'Viktor'},
        {id: 2, name: 'Kostya'},
        {id: 3, name: 'Sergei'}
    ]
}

const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;
